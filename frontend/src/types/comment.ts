export interface Comment {
    id: string;

    authorId: string;

    authorLogin: string;

    authorFirstName: string;

    authorLastName: string;

    profileUserId: string;

    content: string;

    createdAt: string;

    updatedAt: string;
}